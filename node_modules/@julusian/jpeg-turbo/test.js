const jp = require('.')

const opts = {
  format: jp.FORMAT_BGRA,
  width: 96,
  height: 96,
  subsampling: jp.SAMP_420,
  quality: 83
}
const size = jp.bufferSize(opts)
console.log('size', size)

const raw = Buffer.alloc(96 * 96 * 4)

const tmp = Buffer.alloc(size)
// while(true) {
const res = jp.compressSync(raw, tmp, opts)

console.log(res.length)
// }
// const res = jp.compress(raw, tmp, {
//   format: jp.FORMAT_ARGB,
//   width: 10000,
//   height: 10000,
// }, (img, err) => {
//   console.log(img, err)
// })
// console.log(res)
